---
hosts: nginx
become: yes
tasks:
- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: ensure nginx is running
  service:
    name: nginx
    state: started
    enabled: yes